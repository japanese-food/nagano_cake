<h2>注文履歴詳細</h2>

<div>
    
  <div>
      
     <div>
         
         <div>
             <div>購入者</div>
             <div><%= order.name %></div>
         </div>
         <div>
             <div>注文日</div>
             <div> <%= full_address(@order) %></div>
         </div>
         <div>
             <div>配送先</div>
             <div> <%= full_address(@order) %></div>
         </div>
         <div>
             <div>支払方法</div>
             <div> <%= @order.payment_method %></div>
         </div>
         <div>
              <%= form_for(@order, url: admin_order_path, method: :patch) do |f| %>
             <div>注文ステータス</div>
             <div><%= f.select :order_status, Order.order_statuses.keys %></div>
             <div><%= f.submit "更新" %></div>
         </div>
     </div>


            <div>
                <table>
                    <thead>
                        <tr>
                            <th>商品名</th>
                            <th>単価（税込）</th>
                            <th>数量</th>
                            <th>小計</th>
                            <th>制作ステータス</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                        </tr>
                    </tbody>
                </table>
                <div>商品合計</div>
                ¥<%= total_price(@order_details).to_s(:delimited) %>
            　　<div>送料</div>
            　　¥<%= @order.postage.to_s(:delimited) %>
            　　<div>請求金額合計</div>
            　　¥<%= @order.total_price.to_s(:delimited) %>
            </div>
    </div>
</div>